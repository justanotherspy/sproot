schema_version: 1
identity:
  git_user_name: "sproot CI"
  git_user_email: "ci@example.com"
  git_default_branch: main
  gh_username: justanotherspy

targets:
  base:
    phases:
      - type: cmd
        name: base-echo
        run: "echo 'base target ok'"
        check: "true"
  web:
    extends: base
    phases:
      - type: cmd
        name: web-echo
        run: "echo 'web target ok'"
        check: "true"
